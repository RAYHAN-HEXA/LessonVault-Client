// Analytics tracking helpers
export const analyticsUtils = {
  trackEvent: (eventName, data) => console.log('Event tracked:', eventName),
  trackPageView: (page) => console.log('Page view:', page),
};
